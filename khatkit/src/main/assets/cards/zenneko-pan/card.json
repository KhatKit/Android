{
  "name": "zenneko_pan",
  "version": "1.0.0",
  "description": "网盘解析：识别（identify）/打开（open）/列出目录（list）/获取文件直链（direct_link）；仅 direct_link 成功扣费 ¥0.02/次",
  "author": "khatkit",
  "license": "MIT",
  "engine": "lua",
  "entry": { "lua": "main.lua" },
  "privilege": "none",
  "requires": {
    "bridges": ["tool", "ui", "store"],
    "libs": [],
    "bins": [],
    "env": []
  },
  "network": { "allow": ["zenneko.top"] },
  "parameters": {
    "type": "object",
    "properties": {
      "operation": { "type": "string", "enum": ["identify", "open", "list", "direct_link"], "description": "操作：identify（识别）/ open（打开）/ list（列目录）/ direct_link（直链，成功扣费）" },
      "url": { "type": "string", "description": "网盘分享链接" },
      "passcode": { "type": "string", "description": "提取码（可选）" },
      "file_id": { "type": "string", "description": "direct_link 必填：文件 ID（来自 list.items[].id）" },
      "parent_id": { "type": "string", "description": "list 时进入子目录的目录 ID（可选）" },
      "cookie": { "type": "string", "description": "网盘 Cookie（需要登录态的网盘操作时传，可选）" },
      "access_token": { "type": "string", "description": "阿里云盘凭证（provider=aliyun 时必传，可选）" },
      "token": { "type": "string", "description": "123 盘凭证（provider=pan123 时必传，可选）" }
    },
    "required": ["operation", "url"]
  },
  "ui": {
    "operation": { "source": "ui", "widget": "select" },
    "url": { "source": "ui", "widget": "input" },
    "passcode": { "source": "ui", "widget": "input" }
  },
  "tags": { "domain": "net", "action": "read", "scene": "work" },
  "compliance": { "risk": "medium", "note": "付费接口：direct_link 成功扣费 ¥0.02/次（失败不扣费）；请仅解析你有权访问的分享链接" },
  "store": { "quota_mb": 1, "secret": true }
}
